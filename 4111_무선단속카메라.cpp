#include<iostream>
using namespace std;

int t, ret;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	int tc;
	cin >> tc;
	for (int testCase = 1; testCase <= tc; testCase++) {


		cout << "#" << testCase << " " << ret << endl;
	}

	return 0;
}